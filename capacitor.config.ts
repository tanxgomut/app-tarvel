import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.gomot',
  appName: 'one ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
